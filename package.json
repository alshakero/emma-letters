{
  "name": "emma-letters",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist --dotfiles",
    "generate:jingles": "node scripts/generate-jingles.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
